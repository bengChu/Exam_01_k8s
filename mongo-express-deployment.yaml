apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-express-deployment
  labels:
    app: mongo-express
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-express
  template:
    metadata:
      labels:
        app: mongo-express
    spec:
      containers:
      - name: mongo-express
        image: mongo-express:latest # 👈 ใช้ image mongo-express
        ports:
        - containerPort: 8081
        env:
          - name: ME_CONFIG_MONGODB_SERVER
            value: "mongodb-service" # 👈 ต้องใช้ชื่อ service ของ MongoDB ที่เราสร้างไว้
          - name: ME_CONFIG_MONGODB_PORT
            value: "27017"